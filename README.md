# ALPHA System - AI-Powered Life Management

一个完整的 AI Agent 系统，将人工智能融入你的日常生活。通过三个自动化 Agent，帮助你获取前沿信息、系统学习投资知识、做出更好的投资决策。

## 🎯 核心功能

### 三大 Agent

**1. 信息获取 Agent**
- 从 HackerNews、Product Hunt 等源自动爬取最新信息
- 用 LLM 智能分类和评分
- 每天早上 8 点自动生成摘要
- 支持自定义兴趣和关键词

**2. 学习分析 Agent**
- 设计循环的学习路径：Web3 → 美股 → 量化投资
- 每天中午 12 点生成个性化学习内容
- 解释复杂概念、分析真实案例
- 追踪学习进度和知识掌握情况

**3. 投资决策 Agent**
- 监控 Web3 和美股市场
- 每天下午 3 点生成投资信号
- 管理投资组合和交易历史
- 提供风险评估和目标价格

## 🛠️ 技术栈

**后端**
- Node.js + Express
- tRPC（类型安全的 API）
- Drizzle ORM（数据库）
- MySQL / TiDB（数据存储）

**前端**
- React 19
- TypeScript
- Tailwind CSS 4
- shadcn/ui 组件库

**AI 集成**
- OpenAI GPT-4 / GPT-4 Turbo
- Google Gemini
- LLM 调用和提示工程

**部署**
- Docker 支持
- Manus 内置托管
- 支持迁移到 Railway、Render 等

## 📦 项目结构

```
ai-lifeStart-system/
├── client/                 # 前端应用
│   ├── src/
│   │   ├── pages/         # 页面组件
│   │   │   ├── Home.tsx
│   │   │   └── Dashboard.tsx
│   │   ├── components/    # UI 组件
│   │   └── lib/
│   │       └── trpc.ts    # tRPC 客户端
│   └── index.html
├── server/                 # 后端应用
│   ├── services/          # Agent 服务
│   │   ├── informationAgent.ts
│   │   ├── learningAgent.ts
│   │   └── investmentAgent.ts
│   ├── db.ts              # 数据库操作
│   ├── routers.ts         # tRPC 路由
│   └── _core/             # 核心框架
├── drizzle/               # 数据库 schema
│   └── schema.ts
└── package.json
```

## 🚀 快速开始

### 前置要求
- Node.js 22+
- pnpm 10+
- MySQL 8.0+ 或 TiDB

### 安装依赖
```bash
pnpm install
```

### 配置环境变量
创建 `.env.local` 文件：
```env
# 数据库
DATABASE_URL=mysql://user:password@localhost:3306/alpha

# LLM API
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...

# OAuth（可选）
VITE_OAUTH_PORTAL_URL=...
OAUTH_SERVER_URL=...

# 邮件通知
SMTP_HOST=...
SMTP_PORT=...
SMTP_USER=...
SMTP_PASSWORD=...
```

### 初始化数据库
```bash
pnpm db:push
```

### 开发环境
```bash
pnpm dev
```

访问 http://localhost:3000

### 构建生产版本
```bash
pnpm build
pnpm start
```

## 📊 数据库表

- **users** - 用户信息
- **articles** - 爬取的文章
- **daily_summaries** - 每日摘要
- **learning_content** - 学习内容
- **investment_signals** - 投资信号
- **portfolio_items** - 投资组合
- **trade_records** - 交易记录
- **system_messages** - 系统消息
- **user_preferences** - 用户偏好
- **agent_execution_logs** - Agent 执行日志

## 🔄 工作流程

### 信息获取流程
```
定时触发 (08:00)
  ↓
爬取多个数据源
  ↓
用 LLM 分类和评分
  ↓
存储到数据库
  ↓
生成摘要
  ↓
发送通知
```

### 学习流程
```
定时触发 (12:00)
  ↓
根据用户进度生成学习内容
  ↓
解释概念、提供案例
  ↓
存储学习记录
  ↓
发送学习提醒
```

### 投资流程
```
定时触发 (15:00)
  ↓
获取市场数据
  ↓
分析技术面和基本面
  ↓
生成买入/卖出信号
  ↓
存储信号和交易建议
  ↓
发送投资提醒
```

## 🧪 测试

运行所有测试：
```bash
pnpm test
```

运行特定测试：
```bash
pnpm test server/alpha.test.ts
```

## 📝 API 文档

所有 API 都通过 tRPC 暴露，类型安全且自动生成文档。

### 信息 API
- `alpha.information.getArticles` - 获取文章列表
- `alpha.information.getDailySummary` - 获取每日摘要
- `alpha.information.runAgent` - 手动运行信息 Agent

### 学习 API
- `alpha.learning.getContent` - 获取学习内容
- `alpha.learning.getProgress` - 获取学习进度
- `alpha.learning.runAgent` - 手动运行学习 Agent

### 投资 API
- `alpha.investment.getSignals` - 获取投资信号
- `alpha.investment.getPortfolio` - 获取投资组合
- `alpha.investment.createTrade` - 记录交易
- `alpha.investment.runAgent` - 手动运行投资 Agent

## 🔐 安全性

- 所有 API 都通过 tRPC 进行类型检查
- 敏感信息存储在环境变量中
- 数据库连接使用 SSL
- 支持用户认证和授权

## 🌍 部署

### Manus 内置托管
```bash
# 项目已配置为 Manus 部署
# 在 Manus 管理界面点击 "Publish" 按钮即可部署
```

### Railway
```bash
# 连接 GitHub 仓库
# 配置环境变量
# 自动部署
```

### Docker
```bash
docker build -t ai-lifestart-system .
docker run -p 3000:3000 ai-lifestart-system
```

## 📈 性能优化

- 数据库查询已优化，支持大规模数据
- 前端使用 React Query 缓存
- LLM 调用已缓存，避免重复处理
- 支持增量更新和批量操作

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT

## 📞 支持

如有问题，请在 GitHub 提交 Issue。

---

**构建于 Manus 平台** | 最后更新：2026-01-11
